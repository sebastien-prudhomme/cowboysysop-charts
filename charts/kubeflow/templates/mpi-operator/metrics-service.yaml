apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeflow.mpiOperator.metrics.fullname" . }}
  labels:
    {{- include "kubeflow.mpiOperator.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.mpiOperator.metrics.service.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.mpiOperator.metrics.service.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.mpiOperator.metrics.service.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.mpiOperator.metrics.service.type }}
  {{- if and (eq .Values.mpiOperator.metrics.service.type "ClusterIP") .Values.mpiOperator.metrics.service.clusterIP }}
  clusterIP: {{ .Values.mpiOperator.metrics.service.clusterIP }}
  {{- end }}
  ports:
    - port: {{ .Values.mpiOperator.metrics.service.port }}
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "kubeflow.mpiOperator.selectorLabels" . | nindent 4 }}
