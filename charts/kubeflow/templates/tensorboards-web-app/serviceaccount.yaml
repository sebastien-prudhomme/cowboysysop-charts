{{- if .Values.tensorboardsWebApp.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubeflow.tensorboardsWebApp.serviceAccountName" . }}
  labels:
    {{- include "kubeflow.tensorboardsWebApp.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.tensorboardsWebApp.serviceAccount.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.tensorboardsWebApp.serviceAccount.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.tensorboardsWebApp.serviceAccount.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end -}}
