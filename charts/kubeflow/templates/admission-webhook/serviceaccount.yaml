{{- if .Values.admissionWebhook.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubeflow.admissionWebhook.serviceAccountName" . }}
  labels:
    {{- include "kubeflow.admissionWebhook.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.admissionWebhook.serviceAccount.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.admissionWebhook.serviceAccount.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.admissionWebhook.serviceAccount.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end -}}
