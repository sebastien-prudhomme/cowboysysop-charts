{{- if .Values.jupyterWebApp.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubeflow.jupyterWebApp.serviceAccountName" . }}
  labels:
    {{- include "kubeflow.jupyterWebApp.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.jupyterWebApp.serviceAccount.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.jupyterWebApp.serviceAccount.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.jupyterWebApp.serviceAccount.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end -}}
