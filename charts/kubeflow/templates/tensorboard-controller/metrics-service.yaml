apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeflow.tensorboardController.metrics.fullname" . }}
  labels:
    {{- include "kubeflow.tensorboardController.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.tensorboardController.metrics.service.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.tensorboardController.metrics.service.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.tensorboardController.metrics.service.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.tensorboardController.metrics.service.type }}
  {{- if and (eq .Values.tensorboardController.metrics.service.type "ClusterIP") .Values.tensorboardController.metrics.service.clusterIP }}
  clusterIP: {{ .Values.tensorboardController.metrics.service.clusterIP }}
  {{- end }}
  ports:
    - port: {{ .Values.tensorboardController.metrics.service.port }}
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "kubeflow.tensorboardController.selectorLabels" . | nindent 4 }}
