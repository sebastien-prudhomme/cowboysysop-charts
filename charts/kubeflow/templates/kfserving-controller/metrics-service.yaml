apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeflow.kfservingController.metrics.fullname" . }}
  labels:
    {{- include "kubeflow.kfservingController.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.kfservingController.metrics.service.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.kfservingController.metrics.service.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.kfservingController.metrics.service.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.kfservingController.metrics.service.type }}
  {{- if and (eq .Values.kfservingController.metrics.service.type "ClusterIP") .Values.kfservingController.metrics.service.clusterIP }}
  clusterIP: {{ .Values.kfservingController.metrics.service.clusterIP }}
  {{- end }}
  ports:
    - port: {{ .Values.kfservingController.metrics.service.port }}
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "kubeflow.kfservingController.selectorLabels" . | nindent 4 }}
