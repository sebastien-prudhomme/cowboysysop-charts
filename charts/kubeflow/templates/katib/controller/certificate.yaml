apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubeflow.katib.controller.fullname" . }}
  labels:
    {{- include "kubeflow.katib.controller.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  commonName: {{ include "kubeflow.katib.controller.fullname" . }}.{{ .Release.Namespace }}
  dnsNames:
    - {{ include "kubeflow.katib.controller.fullname" . }}.{{ .Release.Namespace }}
  secretName: {{ include "kubeflow.katib.controller.tls.secretName" . }}
  issuerRef:
    kind: Issuer
    name: {{ include "kubeflow.katib.controller.fullname" . }}
