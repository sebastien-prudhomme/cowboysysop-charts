apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeflow.katib.controller.metrics.fullname" . }}
  labels:
    {{- include "kubeflow.katib.controller.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.katib.controller.metrics.service.annotations .Values.commonAnnotations }}
  annotations:
    {{- if .Values.katib.controller.metrics.service.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.katib.controller.metrics.service.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.katib.controller.metrics.service.type }}
  {{- if and (eq .Values.katib.controller.metrics.service.type "ClusterIP") .Values.katib.controller.metrics.service.clusterIP }}
  clusterIP: {{ .Values.katib.controller.metrics.service.clusterIP }}
  {{- end }}
  ports:
    - port: {{ .Values.katib.controller.metrics.service.port }}
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "kubeflow.katib.controller.selectorLabels" . | nindent 4 }}
