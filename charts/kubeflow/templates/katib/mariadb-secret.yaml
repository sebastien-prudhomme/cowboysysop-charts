{{- if and .Values.katib.externalMariadb.enabled (not .Values.katib.externalMariadb.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kubeflow.katib.mariadb.secretName" . }}
  labels:
    {{- include "kubeflow.katib.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  mariadb-password: {{ .Values.katib.externalMariadb.password | b64enc | quote }}
{{- end }}
