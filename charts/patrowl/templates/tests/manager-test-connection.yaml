apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "patrowl.manager.fullname" . }}-test-connection"
  labels:
    {{- include "patrowl.manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "patrowl.manager.fullname" . }}:{{ .Values.manager.service.port }}']
  restartPolicy: Never
