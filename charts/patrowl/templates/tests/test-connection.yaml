apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "patrowl.fullname" . }}-test-connection"
  labels:
    {{- include "patrowl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "patrowl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
