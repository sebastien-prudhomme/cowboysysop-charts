apiVersion: v1
kind: Service
metadata:
  name: {{ include "patrowl.owldnsEngine.fullname" . }}
  labels:
    {{- include "patrowl.owldnsEngine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.owldnsEngine.service.type }}
  ports:
    - port: {{ .Values.owldnsEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "patrowl.owldnsEngine.selectorLabels" . | nindent 4 }}
