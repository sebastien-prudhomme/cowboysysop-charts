apiVersion: v1
kind: Service
metadata:
  name: {{ include "patrowl.arachniEngine.fullname" . }}
  labels:
    {{- include "patrowl.arachniEngine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.arachniEngine.service.type }}
  ports:
    - port: {{ .Values.arachniEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "patrowl.arachniEngine.selectorLabels" . | nindent 4 }}
