apiVersion: v1
kind: Service
metadata:
  name: {{ include "patrowl.nmapEngine.fullname" . }}
  labels:
    {{- include "patrowl.nmapEngine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.nmapEngine.service.type }}
  ports:
    - port: {{ .Values.nmapEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "patrowl.nmapEngine.selectorLabels" . | nindent 4 }}
