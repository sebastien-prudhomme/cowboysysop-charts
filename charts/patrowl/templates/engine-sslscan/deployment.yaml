apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "patrowl.engineSslscan.fullname" . }}
  labels:
    {{- include "patrowl.engineSslscan.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.engineSslscan.replicaCount }}
  selector:
    matchLabels:
      {{- include "patrowl.engineSslscan.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "patrowl.engineSslscan.selectorLabels" . | nindent 8 }}
        {{- with .Values.engineSslscan.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.engineSslscan.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "patrowl.engineSslscan.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.engineSslscan.podSecurityContext | nindent 8 }}
      containers:
        - name: engine-sslscan
          securityContext:
            {{- toYaml .Values.engineSslscan.securityContext | nindent 12 }}
          image: "{{ .Values.engineSslscan.image.repository }}:{{ .Values.engineSslscan.image.tag }}"
          imagePullPolicy: {{ .Values.engineSslscan.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5014
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /engines/sslscan/liveness
              port: http
          readinessProbe:
            httpGet:
              path: /engines/sslscan/readiness
              port: http
          resources:
            {{- toYaml .Values.engineSslscan.resources | nindent 12 }}
      {{- with .Values.engineSslscan.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.engineSslscan.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.engineSslscan.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
