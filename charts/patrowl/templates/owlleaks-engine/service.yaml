apiVersion: v1
kind: Service
metadata:
  name: {{ include "patrowl.owlleaksEngine.fullname" . }}
  labels:
    {{- include "patrowl.owlleaksEngine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.owlleaksEngine.service.type }}
  ports:
    - port: {{ .Values.owlleaksEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "patrowl.owlleaksEngine.selectorLabels" . | nindent 4 }}
