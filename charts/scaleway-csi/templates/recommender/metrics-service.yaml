apiVersion: v1
kind: Service
metadata:
  name: {{ include "scaleway-csi.recommender.metrics.fullname" . }}
  labels:
    {{- include "scaleway-csi.recommender.labels" . | nindent 4 }}
spec:
  type: {{ .Values.recommender.metrics.service.type }}
  ports:
    - port: {{ .Values.recommender.metrics.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "scaleway-csi.recommender.selectorLabels" . | nindent 4 }}
