apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleway-csi.node.metrics.fullname" . }}-test-connection"
  labels:
    {{- include "scaleway-csi.node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaleway-csi.node.metrics.fullname" . }}:{{ .Values.node.metrics.service.port }}/metrics']
  restartPolicy: Never
