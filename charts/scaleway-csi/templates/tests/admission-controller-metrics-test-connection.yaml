apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleway-csi.controller.metrics.fullname" . }}-test-connection"
  labels:
    {{- include "scaleway-csi.controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaleway-csi.controller.metrics.fullname" . }}:{{ .Values.controller.metrics.service.port }}/metrics']
  restartPolicy: Never
