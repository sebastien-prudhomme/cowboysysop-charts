apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleway-csi.controller.fullname" . }}-test-connection"
  labels:
    {{- include "scaleway-csi.controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      # args: ['https://{{ include "scaleway-csi.controller.fullname" . }}:{{ .Values.controller.service.port }}']
      args: ['https://vpa-webhook:443']
  restartPolicy: Never
