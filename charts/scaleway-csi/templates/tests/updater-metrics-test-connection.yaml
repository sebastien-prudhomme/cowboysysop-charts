apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleway-csi.updater.metrics.fullname" . }}-test-connection"
  labels:
    {{- include "scaleway-csi.updater.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaleway-csi.updater.metrics.fullname" . }}:{{ .Values.updater.metrics.service.port }}/metrics']
  restartPolicy: Never
