apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubeflow-pipelines.cacheServer.fullname" . }}
  labels:
    {{- include "kubeflow-pipelines.cacheServer.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  commonName: {{ include "kubeflow-pipelines.cacheServer.fullname" . }}.{{ .Release.Namespace }}.svc
  dnsNames:
    - {{ include "kubeflow-pipelines.cacheServer.fullname" . }}.{{ .Release.Namespace }}.svc
  secretName: {{ include "kubeflow-pipelines.cacheServer.tls.secretName" . }}
  issuerRef:
    kind: Issuer
    name: {{ include "kubeflow-pipelines.cacheServer.fullname" . }}
  isCA: true
