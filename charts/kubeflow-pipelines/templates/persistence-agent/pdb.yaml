{{- if .Values.persistenceAgent.pdb.create }}
apiVersion: {{ include "common.capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "kubeflow-pipelines.persistenceAgent.fullname" . }}
  labels:
    {{- include "kubeflow-pipelines.persistenceAgent.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.persistenceAgent.pdb.minAvailable }}
  minAvailable: {{ .Values.persistenceAgent.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.persistenceAgent.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.persistenceAgent.pdb.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "kubeflow-pipelines.persistenceAgent.selectorLabels" . | nindent 6 }}
{{- end }}
