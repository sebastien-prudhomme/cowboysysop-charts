{{- if .Values.recommender.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jitsi-meet.recommender.serviceAccountName" . }}
  labels:
    {{- include "jitsi-meet.recommender.labels" . | nindent 4 }}
  {{- with .Values.recommender.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end -}}
