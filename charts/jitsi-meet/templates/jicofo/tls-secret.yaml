{{- if (not .Values.jicofo.tls.existingSecret) }}
{{- $ca := genCA (include "jitsi-meet.jicofo.fullname" .) 365 }}
# {{- $cn := printf "%s.%s.svc" (include "jitsi-meet.jicofo.fullname" .) .Release.Namespace }}
{{- $cn := printf "%s.%s.svc" "vpa-webhook" .Release.Namespace }}
{{- $cert := genSignedCert $cn nil (list $cn) 365 $ca }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "jitsi-meet.jicofo.tls.secretName" . }}
  labels:
    {{- include "jitsi-meet.jicofo.labels" . | nindent 4 }}
type: Opaque
data:
  ca.crt: {{ default $ca.Cert .Values.jicofo.tls.caCert | b64enc | quote }}
  tls.crt: {{ default $cert.Cert .Values.jicofo.tls.cert | b64enc | quote }}
  tls.key: {{ default $cert.Key .Values.jicofo.tls.key | b64enc | quote }}
{{- end }}
