apiVersion: v1
kind: Service
metadata:
  name: {{ include "jitsi-meet.jicofo.metrics.fullname" . }}
  labels:
    {{- include "jitsi-meet.jicofo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.jicofo.metrics.service.type }}
  ports:
    - port: {{ .Values.jicofo.metrics.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "jitsi-meet.jicofo.selectorLabels" . | nindent 4 }}
