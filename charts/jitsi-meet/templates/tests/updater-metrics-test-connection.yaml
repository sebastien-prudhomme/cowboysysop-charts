apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jitsi-meet.web.metrics.fullname" . }}-test-connection"
  labels:
    {{- include "jitsi-meet.web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jitsi-meet.web.metrics.fullname" . }}:{{ .Values.web.metrics.service.port }}/metrics']
  restartPolicy: Never
