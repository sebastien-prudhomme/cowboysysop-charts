apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jitsi-meet.jicofo.fullname" . }}-test-connection"
  labels:
    {{- include "jitsi-meet.jicofo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jitsi-meet.jicofo.fullname" . }}:{{ .Values.jicofo.service.port }}']
  restartPolicy: Never
