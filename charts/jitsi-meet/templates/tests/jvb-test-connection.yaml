apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jitsi-meet.jvb.fullname" . }}-test-connection"
  labels:
    {{- include "jitsi-meet.jvb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jitsi-meet.jvb.fullname" . }}:{{ .Values.jvb.service.port }}']
  restartPolicy: Never
