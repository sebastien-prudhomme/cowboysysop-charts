apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jitsi-meet.admissionController.fullname" . }}-test-connection"
  labels:
    {{- include "jitsi-meet.admissionController.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      # args: ['https://{{ include "jitsi-meet.admissionController.fullname" . }}:{{ .Values.admissionController.service.port }}']
      args: ['https://vpa-webhook:443']
  restartPolicy: Never
