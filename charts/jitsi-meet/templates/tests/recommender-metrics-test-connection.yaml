apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jitsi-meet.jvb.metrics.fullname" . }}-test-connection"
  labels:
    {{- include "jitsi-meet.jvb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jitsi-meet.jvb.metrics.fullname" . }}:{{ .Values.jvb.metrics.service.port }}/metrics']
  restartPolicy: Never
