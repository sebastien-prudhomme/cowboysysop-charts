apiVersion: v1
kind: Service
metadata:
  name: {{ include "jitsi-meet.admissionController.metrics.fullname" . }}
  labels:
    {{- include "jitsi-meet.admissionController.labels" . | nindent 4 }}
spec:
  type: {{ .Values.admissionController.metrics.service.type }}
  ports:
    - port: {{ .Values.admissionController.metrics.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "jitsi-meet.admissionController.selectorLabels" . | nindent 4 }}
