apiVersion: v1
kind: Service
metadata:
  name: {{ include "jitsi-meet.web.metrics.fullname" . }}
  labels:
    {{- include "jitsi-meet.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.metrics.service.type }}
  ports:
    - port: {{ .Values.web.metrics.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "jitsi-meet.web.selectorLabels" . | nindent 4 }}
