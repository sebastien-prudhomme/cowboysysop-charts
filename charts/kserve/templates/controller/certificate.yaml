apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kserve.controller.fullname" . }}
  labels:
    {{- include "kserve.controller.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  commonName: {{ include "kserve.controller.fullname" . }}.{{ .Release.Namespace }}.svc
  dnsNames:
    - {{ include "kserve.controller.fullname" . }}.{{ .Release.Namespace }}.svc
  secretName: {{ include "kserve.controller.tls.secretName" . }}
  issuerRef:
    kind: Issuer
    name: {{ include "kserve.controller.fullname" . }}
  isCA: true
